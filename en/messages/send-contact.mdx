---
title: "Send contact"
description: "Send contacts via WhatsApp"
openapi: "/en/messages/openapi-contact.json POST /v1/channels/{channelId}/messages"
---

import { ChannelSupport } from '/snippets/channel-support.mdx';

## Overview

Sends a contact to a recipient. The `content.type` field must be `CONTACT`. You can send multiple phone numbers in the `phones` array.

<ChannelSupport lang="en" instagram="no" messenger="no" />

<Note>
  Details of the WhatsApp Official rule in [conversation window](/en/conversation-window). See the [capability matrix](/en/channels/overview) for every channel.
</Note>
