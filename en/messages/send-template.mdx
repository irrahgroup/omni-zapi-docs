---
title: "Send template"
description: "Send Meta-approved templates"
openapi: "/en/messages/openapi-template.json POST /v1/channels/{channelId}/messages"
---

import { ChannelSupport } from '/snippets/channel-support.mdx';

## Overview

Sends a Meta-approved message template to a contact. This is the **only way** to send messages outside the [conversation window](/en/conversation-window) (24 hours).

The `content.type` field must be `TEMPLATE` and the template is sent within `attachments`.

<ChannelSupport lang="en" web="no" instagram="no" messenger="no" telegram="no" />

<Warning>
  Templates are a **WhatsApp Official only** feature. They do not exist on the other channels — and they are not needed there, because there is no window to reopen. See the [capability matrix](/en/channels/overview).
</Warning>

<Warning>
  The template must have `APPROVED` status to work. Use the [Templates](/en/templates/introduction) endpoints to create and manage your templates.
</Warning>
