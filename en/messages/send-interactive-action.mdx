---
title: "Send action buttons"
description: "Send messages with action buttons (URL and call)"
openapi: "/en/messages/openapi-interactive-action.json POST /v1/channels/{channelId}/messages"
---

import { ChannelSupport } from '/snippets/channel-support.mdx';

## Overview

Sends a message with action buttons to a contact. The `content.type` field must be `INTERACTIVE_ACTION`.

Available button types:

- **URL** — Opens a link when clicked
- **CALL** — Calls a number when clicked

The `header` and `footer` fields are optional.

<ChannelSupport lang="en" />

<Note>
  Details of the WhatsApp Official rule in [conversation window](/en/conversation-window). See the [capability matrix](/en/channels/overview) for every channel.
</Note>
