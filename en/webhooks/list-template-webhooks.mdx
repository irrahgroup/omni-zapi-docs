---
title: "List template webhooks"
description: "List the tenant's template webhooks"
openapi: "/en/webhooks/openapi-template.json GET /v1/webhooks"
---

import { EnterpriseText } from '/snippets/notes.mdx';

## Overview

Returns the tenant's enabled template webhooks, scoped by the `Authorization` header — there is no cross-tenant access.

Authentication credentials and the signing `secret` are **not returned** for security reasons.

<Note>
  <EnterpriseText lang="en" />
</Note>
