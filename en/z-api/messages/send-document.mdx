---
title: "Send document"
description: "Send documents (PDF, DOC, XLS, etc) via WhatsApp"
openapi: "/en/z-api/openapi.json POST /send-document/{extension}"
---

import { ZapiResponseText } from '/snippets/notes.mdx';

## Overview

Method for sending documents. The file extension must be specified in the URL path (e.g., `/send-document/pdf`). You can send via **URL** or **Base64**.

<Warning>
  When sending as Base64, you must include the prefix with the document type, for example: `data:application/pdf;base64,`.
</Warning>

<Note>
  <ZapiResponseText lang="en" />
</Note>

<Note>
  File size limits and supported formats follow WhatsApp policies. See the [official WhatsApp documentation](https://developers.facebook.com/docs/whatsapp/api/media) for details.
</Note>
