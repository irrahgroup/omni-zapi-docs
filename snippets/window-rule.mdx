export const WindowRule = ({ lang = 'pt', template, href }) => {
  const L = {
    pt: {
      title: 'Só dentro da janela de 24 horas',
      body: 'Este é um tipo de mensagem, não um template: não passa por aprovação da Meta e pode ser enviado assim que você o integrar. Em compensação, só sai enquanto a janela estiver aberta — ou seja, depois que o cliente te escreveu.',
      window: 'janela de conversa',
      windowHref: '/conversation-window',
      withTemplate: 'Fora da janela, o caminho equivalente é o template',
      noTemplate: 'Fora da janela não há caminho: este tipo não existe como template. Para reabrir a conversa, mande um template e espere a resposta do cliente.',
    },
    en: {
      title: 'Inside the 24-hour window only',
      body: 'This is a message type, not a template: it goes through no Meta review and can be sent as soon as you integrate it. In exchange, it only goes out while the window is open — that is, after the customer has written to you.',
      window: 'conversation window',
      windowHref: '/en/conversation-window',
      withTemplate: 'Outside the window, the equivalent path is the template',
      noTemplate: 'Outside the window there is no path: this type does not exist as a template. To reopen the conversation, send a template and wait for the customer to reply.',
    },
    es: {
      title: 'Solo dentro de la ventana de 24 horas',
      body: 'Este es un tipo de mensaje, no una plantilla: no pasa por aprobación de Meta y puedes enviarlo apenas lo integres. A cambio, solo sale mientras la ventana esté abierta — es decir, después de que el cliente te haya escrito.',
      window: 'ventana de conversación',
      windowHref: '/es/conversation-window',
      withTemplate: 'Fuera de la ventana, el camino equivalente es la plantilla',
      noTemplate: 'Fuera de la ventana no hay camino: este tipo no existe como plantilla. Para reabrir la conversación, manda una plantilla y espera la respuesta del cliente.',
    },
  }[lang];

  return (
    <div
      style={{
        border: '1px solid var(--zp-border, #e4e7e1)',
        borderLeft: '3px solid #477722',
        borderRadius: 8,
        padding: '12px 14px',
        margin: '16px 0',
        fontSize: 14,
        lineHeight: 1.55,
      }}
    >
      <div style={{ fontWeight: 700, marginBottom: 4 }}>{L.title}</div>
      <div>
        {L.body} <a href={L.windowHref}>{L.window}</a>.
      </div>
      <div style={{ marginTop: 6 }}>
        {template ? (
          <>
            {L.withTemplate} <a href={href}>{template}</a>.
          </>
        ) : (
          L.noTemplate
        )}
      </div>
    </div>
  );
};
