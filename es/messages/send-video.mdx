---
title: "Enviar vídeo"
description: "Envía mensajes con vídeo"
openapi: "/es/messages/openapi-video.json POST /v1/channels/{channelId}/messages"
---

import { ChannelSupport } from '/snippets/channel-support.mdx';

## Conceptos

Envía un mensaje con vídeo a un contacto. El campo `content.type` debe ser `VIDEO` y el vídeo se envía mediante una URL dentro del array `attachments`.

Opcionalmente puedes incluir un `caption` (pie de vídeo) junto con el vídeo.

<ChannelSupport lang="es" />

<Note>
  Detalles de la regla de WhatsApp Oficial en [ventana de conversación](/es/conversation-window). Consulta la [matriz de capacidades](/es/channels/overview) para todos los canales.
</Note>
