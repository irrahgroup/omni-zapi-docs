---
title: "Consultar webhook de plantilla"
description: "Consulta los datos de un webhook de plantilla concreto"
openapi: "/es/webhooks/openapi-template.json GET /v1/webhooks/{webhookId}"
---

import { EnterpriseText } from '/snippets/notes.mdx';

## Conceptos

Devuelve los datos de un webhook de plantilla concreto del tenant. Igual que en el listado, las credenciales de autenticación y el `secret` **no se devuelven** por seguridad.

<Note>
  <EnterpriseText lang="es" />
</Note>

<Note>
  El `webhookId` se obtiene en la respuesta del endpoint [Crear webhook de plantilla](/es/webhooks/create-template-webhook) o [Listar webhooks de plantilla](/es/webhooks/list-template-webhooks).
</Note>
