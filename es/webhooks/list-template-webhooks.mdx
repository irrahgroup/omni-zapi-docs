---
title: "Listar webhooks de plantilla"
description: "Lista los webhooks de plantilla del tenant"
openapi: "/es/webhooks/openapi-template.json GET /v1/webhooks"
---

import { EnterpriseText } from '/snippets/notes.mdx';

## Conceptos

Devuelve los webhooks de plantilla habilitados del tenant, limitados al alcance del `Authorization`: no hay acceso cross-tenant.

Las credenciales de autenticación y el `secret` de firma **no se devuelven** por seguridad.

<Note>
  <EnterpriseText lang="es" />
</Note>
