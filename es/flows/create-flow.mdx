---
title: "Crear flow"
description: "Crea un flow a partir de su flow_json"
openapi: "/es/flows/openapi.json POST /whatsapp/businesses/{wabaId}/flows"
---

## Conceptos

Crea un flow en la WABA. El `flow_json` es la definición de las pantallas, enviada como **string**, no como objeto.

Con `publish: true` el flow nace publicado. Si no, queda en `DRAFT`, que es el único estado en el que todavía puedes cambiar el `flow_json`.

<Warning>
  Si alguna pantalla usa la acción `data_exchange`, el campo `endpoint_uri` es obligatorio.
</Warning>

<Note>
  La estructura del `flow_json` está en la [Introducción](/es/flows/introduction). Lo que Meta valida antes de publicar (ID de pantalla sin números, `SUCCESS` reservado, nombre de campo en minúsculas, pantalla final obligatoria) está en [Componentes y reglas](/es/flows/components).
</Note>
