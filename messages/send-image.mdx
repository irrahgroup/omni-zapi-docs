---
title: "Enviar imagem"
description: "Envie mensagens com imagem"
openapi: "/pt/messages/openapi-image.json POST /v1/channels/{channelId}/messages"
---

import { ChannelSupport } from '/snippets/channel-support.mdx';

## Conceituação

Envia uma mensagem com imagem para um contato. O campo `content.type` deve ser `IMAGE` e a imagem é enviada via URL no array `attachments`.

Opcionalmente, você pode incluir uma `caption` (legenda) junto à imagem.

<ChannelSupport type="IMAGE" lang="pt" />

<Note>
  Detalhes da regra da Meta Cloud API em [janela de conversa](/conversation-window). Veja [o que cada canal aceita](/channels/message-support) para todos os canais.
</Note>
