---
title: "Enviar contato"
description: "Envie contatos via WhatsApp"
openapi: "/pt/messages/openapi-contact.json POST /v1/channels/{channelId}/messages"
---

import { ChannelSupport } from '/snippets/channel-support.mdx';

<Warning>
  **Em implementação.** Hoje só **um contato por mensagem** funciona: o `attachments` aceita uma lista, mas do segundo item em diante nada é enviado.

  Também só levamos `name` e `phones`. A [Meta aceita](https://developers.facebook.com/documentation/business-messaging/whatsapp/messages/contacts-messages) até **257 contatos** no mesmo envio, e em cada um também e-mail, endereço, organização, aniversário e site.

  Estamos ampliando o endpoint. Até lá, mande um contato por requisição.
</Warning>

## Conceituação

Envia um contato para um destinatário. O campo `content.type` deve ser `CONTACT`. Você pode enviar múltiplos telefones no array `phones`.

<ChannelSupport type="CONTACT" lang="pt" />

<Note>
  Detalhes da regra da Meta Cloud API em [janela de conversa](/conversation-window). Veja [o que cada canal aceita](/channels/message-support) para todos os canais.
</Note>
