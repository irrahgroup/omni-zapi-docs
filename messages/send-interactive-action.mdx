---
title: "Enviar botões de ação"
description: "Envie mensagens com botões de ação (URL e ligar)"
openapi: "/pt/messages/openapi-interactive-action.json POST /v1/channels/{channelId}/messages"
---

import { ChannelSupport } from '/snippets/channel-support.mdx';

## Conceituação

Envia uma mensagem com botões de ação para um contato. O campo `content.type` deve ser `INTERACTIVE_ACTION`.

Tipos de botão disponíveis:

- **URL** — Abre um link ao clicar
- **CALL** — Liga para um número ao clicar

Os campos `header` e `footer` são opcionais.

<ChannelSupport type="INTERACTIVE_ACTION" lang="pt" />

<Note>
  Detalhes da regra da Meta Cloud API em [janela de conversa](/conversation-window). Veja [o que cada canal aceita](/channels/message-support) para todos os canais.
</Note>
